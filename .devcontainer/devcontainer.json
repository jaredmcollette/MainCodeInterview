{
	"name": "Mainrun",
	"build": {
		"dockerfile": "Dockerfile",
		"context": "."
	},
	"hostRequirements": {
		"gpu": "optional"
	},
	"remoteUser": "root",
	"runArgs": [
		// Uncomment the next line only when running on a GPU machine
		// "--gpus", "all",
	]
}